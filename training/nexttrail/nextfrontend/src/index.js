import React from 'react';
import ReactDOM from 'react-dom/client';
import NextTrial from './NextTrial';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NextTrial />
  </React.StrictMode>
);

